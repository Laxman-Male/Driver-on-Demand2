package routes

// handlers "dod-backend/handler"
// "net/http"

// "net/http"

func SetupRoutes() {

	// http.HandleFunc("/login", handlers.LoginUser)
	// http.HandleFunc("/get-rate", handlers.GetRateHandler)

}
